# Copy to backend.hcl, replace the bucket name, then initialize with:
# terraform init -backend-config=backend.hcl
bucket       = "REPLACE_WITH_TERRAFORM_STATE_BUCKET"
key          = "tripare/dev/terraform.tfstate"
region       = "ap-south-1"
encrypt      = true
use_lockfile = true

